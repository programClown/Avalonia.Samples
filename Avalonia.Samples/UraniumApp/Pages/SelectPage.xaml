<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             x:Class="UraniumApp.Pages.SelectPage"
             Title="Select">

    <ContentPage.Resources>
        <x:String x:Key="SourceLink">https://github.com/enisn/UraniumUI/blob/develop/demo/UraniumApp/Pages/SelectPage.xaml</x:String>
        <DataTemplate x:Key="RichSelectItemTemplate">
            <Grid Padding="12" ColumnDefinitions="36,*" ColumnSpacing="12">
                <Border WidthRequest="32" HeightRequest="32" StrokeThickness="0" BackgroundColor="{Binding Color}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16" />
                    </Border.StrokeShape>
                    <Label Text="{Binding Initial}" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" />
                </Border>
                <VerticalStackLayout Grid.Column="1" Spacing="2">
                    <Label Text="{Binding Name}" FontAttributes="Bold" />
                    <Label Text="{Binding Description}" FontSize="Micro" Opacity=".65" />
                </VerticalStackLayout>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="CompactSelectSelectedItemTemplate">
            <Grid ColumnDefinitions="24,*" ColumnSpacing="8">
                <Border WidthRequest="22" HeightRequest="22" StrokeThickness="0" BackgroundColor="{Binding Color}" VerticalOptions="Center">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="11" />
                    </Border.StrokeShape>
                    <Label Text="{Binding Initial}" TextColor="White" FontSize="Micro" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" />
                </Border>
                <Label Grid.Column="1" Text="{Binding Name}" FontAttributes="Bold" VerticalOptions="Center" LineBreakMode="TailTruncation" />
            </Grid>
        </DataTemplate>
    </ContentPage.Resources>

    <Grid RowDefinitions="*">
        <material:TabView TabPlacement="Top">
            <material:TabItem Title="Select">
                <ScrollView>
                    <VerticalStackLayout Padding="20" Spacing="20">
                        <Label Text="Select" FontSize="Title" />
                        <Label Text="Overlay-hosted MAUI Select with templated items." FontSize="Micro" Opacity=".6" />

                        <HorizontalStackLayout Padding="0,0,0,8">
                            <Button Text="Source" StyleClass="OutlinedButton" Command="{x:Static uranium:Commands.OpenLinkCommand}" CommandParameter="{StaticResource SourceLink}" ImageSource="{FontImageSource Glyph={x:Static uranium:MaterialSharp.Code}, FontFamily=MaterialSharp, Color={AppThemeBinding {StaticResource OnBackground}, Dark={StaticResource OnBackgroundDark}}}" />
                        </HorizontalStackLayout>

                        <Label Text="Scroll this page, then open the select. The overlay should follow the field's visible screen position." />

                        <uranium:Select
                                WidthRequest="320"
                                Placeholder="Choose a profile"
                                ItemsSource="{Binding Profiles}"
                                SelectedItem="{Binding SelectedProfile}"
                                ItemTemplate="{StaticResource RichSelectItemTemplate}"
                                DropDownBackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource SurfaceDark}}"
                                DropDownBorderColor="{AppThemeBinding Light={StaticResource Outline}, Dark={StaticResource OutlineDark}}"
                                SelectedItemBackgroundColor="{AppThemeBinding Light={StaticResource PrimaryContainer}, Dark={StaticResource PrimaryContainerDark}}"
                                HoveredItemBackgroundColor="{AppThemeBinding Light={StaticResource SurfaceVariant}, Dark={StaticResource SurfaceVariantDark}}"
                                PressedItemBackgroundColor="{AppThemeBinding Light={StaticResource PrimaryContainer}, Dark={StaticResource PrimaryContainerDark}}" />

                        <HorizontalStackLayout Spacing="12">
                            <Button Text="Select Grace in code" StyleClass="OutlinedButton" Clicked="SelectGrace_Clicked" />
                            <Button Text="Clear" StyleClass="TextButton" Clicked="Clear_Clicked" />
                        </HorizontalStackLayout>

                        <BoxView HeightRequest="500" Color="Transparent" />
                    </VerticalStackLayout>
                </ScrollView>
            </material:TabItem>

            <material:TabItem Title="SelectField">
                <ScrollView>
                    <VerticalStackLayout Padding="20" Spacing="20">
                        <Label Text="SelectField" FontSize="Title" />
                        <Label Text="Material InputField wrapper for Select with clear and selected-item templates." FontSize="Micro" Opacity=".6" />

                        <HorizontalStackLayout Padding="0,0,0,8">
                            <Button Text="Source" StyleClass="OutlinedButton" Command="{x:Static uranium:Commands.OpenLinkCommand}" CommandParameter="{StaticResource SourceLink}" ImageSource="{FontImageSource Glyph={x:Static uranium:MaterialSharp.Code}, FontFamily=MaterialSharp, Color={AppThemeBinding {StaticResource OnBackground}, Dark={StaticResource OnBackgroundDark}}}" />
                        </HorizontalStackLayout>

                        <Label Text="Scroll this page, then open the field. The overlay should follow the field's visible screen position." />

                        <material:SelectField
                                Title="Material field"
                                WidthRequest="320"
                                AllowClear="True"
                                ItemsSource="{Binding Profiles}"
                                SelectedItem="{Binding SelectedProfile}"
                                ItemTemplate="{StaticResource RichSelectItemTemplate}"
                                SelectedItemTemplate="{StaticResource CompactSelectSelectedItemTemplate}" />

                        <HorizontalStackLayout Spacing="12">
                            <Button Text="Select Grace in code" StyleClass="OutlinedButton" Clicked="SelectGrace_Clicked" />
                            <Button Text="Clear" StyleClass="TextButton" Clicked="Clear_Clicked" />
                        </HorizontalStackLayout>

                        <BoxView HeightRequest="500" Color="Transparent" />
                    </VerticalStackLayout>
                </ScrollView>
            </material:TabItem>
        </material:TabView>
    </Grid>
</ContentPage>
